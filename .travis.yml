os:
  - linux

language: cpp

dist: trusty

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake3_3.10
      - gcc-7
      - g++-7
      - libboost-program-options1.55-dev
      - libboost-system1.55-dev

script:
  - export CC=gcc-7
  - export CXX=g++-7

  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - make test

